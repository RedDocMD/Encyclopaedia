- if @wiki.errors.any?
  .alert.alert-danger
    %p #{pluralize(@wiki.errors.count, 'error')} prevented this wiki from being created
    %ul
      - @wiki.errors.full_messages.each do |error|
        %li= error
= form_with model: @wiki, local: true do |form|
  = form.text_field :title, placeholder: 'Title', class: 'form-control'
  = form.hidden_field :text
  #editor.my-4
  = submit_tag nil, class: "btn btn-primary"
  = javascript_pack_tag 'editor'