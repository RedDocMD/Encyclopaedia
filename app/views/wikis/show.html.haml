.col-10.mt-2
  - if @wiki.nil?
    %h2(class="text-danger mt-4")= "This page was not found"
  - else 
    - provide :title, @wiki.title
    != @html
    .mt-5.d-flex.flex-row-reverse
      = link_to 'Delete Wiki', wiki_path(@wiki), method: :delete, class: 'btn btn-danger mx-2', data: { confirm: "Are you sure?"} 
      = link_to 'Edit Wiki', edit_wiki_path(@wiki), class: 'btn btn-success mx-2'